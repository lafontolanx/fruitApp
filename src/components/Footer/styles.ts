import styled from "styled-components";

export const Container = styled.div`
    background: var(--white);
    padding: 1rem;
    text-align: center;
    margin-top: 2rem;

    box-shadow: 12px 12px 24px 12px rgba(0, 0, 0, 0.5);
    -webkit-box-shadow: 12px 12px 24px 12px rgba(0, 0, 0, 0.5);
`